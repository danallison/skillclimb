# SkillClimb Environment Configuration
# Copy to .env and fill in values

# --- Postgres (used by docker-compose and DATABASE_URL) ---
POSTGRES_DB=skillclimb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

# --- Database ---
# Local development (host machine → Docker postgres):
DATABASE_URL=postgres://postgres:changeme@localhost:5432/skillclimb
# Docker Compose (set automatically in docker-compose.yml, no need to change):
# DATABASE_URL=postgres://postgres:changeme@postgres:5432/skillclimb

# --- AI Provider ---
# Options: anthropic | openai | ollama | none
# AI_PROVIDER=anthropic

# Anthropic (required if AI_PROVIDER=anthropic)
# ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (required if AI_PROVIDER=openai)
# OPENAI_API_KEY=sk-...

# Ollama (no key needed, defaults to localhost:11434)
# OLLAMA_BASE_URL=http://localhost:11434

# --- OAuth ---
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# --- Auth ---
# JWT secret for session tokens (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# JWT_SECRET=

# --- Server ---
# PORT=3001
# APP_URL=http://localhost:5173

# --- Deployment (docker-compose) ---
# Domain for Caddy auto-HTTPS (set to your domain for TLS, or leave as localhost)
# DOMAIN=localhost
